# Frontend MVC notes

- `views/`: page-level React JSX screens
- `controllers/`: client-side handlers and API helpers
- `models/`: data contracts / mock data / future state models
- `shared/`: reusable UI and layout helpers

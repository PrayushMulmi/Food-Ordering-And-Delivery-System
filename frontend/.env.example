# For local development you can leave this empty and let Vite proxy /api to http://localhost:5050.
# If you deploy the backend separately, set the full backend origin, for example: http://localhost:5050
VITE_API_URL=

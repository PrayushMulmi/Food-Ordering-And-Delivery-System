# Frontend

## Run

```bash
npm install
npm run dev
```

## Notes
- Built with Vite + React JSX
- All page files are `.jsx`
- Reusable UI from the original `components` folder has been consolidated into `src/shared/ui.jsx`
